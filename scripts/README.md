# scripts/ — thin launch helpers

Convenience wrappers around the `brats2026` CLI and nnU-Net commands. Keep logic in the
package (`src/brats2026/`) so it stays unit-testable; scripts here only wire arguments.
